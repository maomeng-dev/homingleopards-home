'use strict';

let totop = require('../common/totop.module');

$(() => {
    let page = {
        eles: {
        },

        init: function () {
            totop.init();
        }
    };

    page.init();
});